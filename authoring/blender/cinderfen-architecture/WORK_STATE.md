# Saved work state: September 20 stair collision correction published

User resumed work. Corner gangways now extend 1.414m into the courtyard, preserving
both wall sockets while moving the attached stair clear of the curtain base.
The previous whole-publication snapshot is retained under
`junction/releases/pre-courtyard-clearance-5efebeab/`. Replacement corner LOD0/1/2
are published with source, editable master, actual reimport views, zero-error and
zero-warning Khronos reports, topology/support audits and standing user approval.
Parent integration owns global registry/campaign/GM regeneration.

The unchanged stair model now has 147 continuous elevated rail blockers derived
from its literal authored pieces. Six focused tests pass, including
sideways containment between posts, rear landing containment, both-flight ascent
and lower clearance, continuous landing turn and exact placed-model evidence.
Shared route floodfill reaches all 13 keep and 13 village
targets. Corner assembly has zero head obstructions or unsupported samples.
Final all-16-stair actual LOD0 triangle audit after parent campaign regeneration
reports zero curtain/gatehouse intersections (previously all16). The regression
binds the audit to each placed transform and the exact published model hashes.

Historical original release details follow; its claim about all geometry clearing
walls was too broad because the original test only checked walking centerlines.

All eight Cinderfen architecture modules are internally accepted and locally
published. Main and companion review/review.json files bind the exact current
build hashes, both contact sheets and all 59 required actual-export images.
24 GLBs pass zero Khronos errors/warnings, zero positional boundary/multi-face
edges and measured opening/support checks. The gatehouse has a real six-metre
passage; paired leaves retain exact hinges and gate_open/gate_close clips.

The complete keep has connected outer/inner perimeters, real corner gangways and
eight switchbacks. Actual assembled LOD1 geometry has zero unsupported samples
and zero head obstructions. Shared navigation reaches 13 keep targets and 13
village targets with closed gates and owner passage pairs. On landscape v2 the
maximum footprint ground error is 0.000061m. Commander, delivery, quartermaster
and original gate IDs remain fixed. Root fixed stair-footprint collision and
aligned authoritative gate footprints to the authored localZ-.3 closed collider.

Main and corner publishers completed. tools/merge_builder_metadata.mjs merged
all eight runtimeReady entries with approved model hashes, exact colliders,
walkable surfaces and gate defaults. Both published-package tests pass. Global
registry and generator are untouched; root activates the frozen composer via
composeCinderfenEnvironment(zone, { architecture: true }). Do not change the
frozen composer release constant. Measured banner attachments are retained in
review/banner-sockets.json. Full delivery details and hashes are in HANDOFF.md.

No architecture Blender process remains active. Preserve the frozen source,
masters, GLBs, material files and receipts. Next assigned bounded task is original
Cinderfen ecology under authoring/blender/cinderfen-nature/: marsh alder, reed
clump, sedge/horsetail ground cover and irregular basalt outcrop. Root owns
landscape, registry and zone integration. Continue on codex/map-readability;
do not switch branches, commit or alter unrelated user work.
