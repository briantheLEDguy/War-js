# Field command table work state

Published 2026-09-20: release `8f77f8a5152929d95d5c`, all 36 keeps and the GM
builder. All-placement support/access, four regional geometry checks and three
actual Cinderfen Game/Player collision presses pass. Frozen authoring evidence
remains unchanged; see `review/runtime-integration-20260920.json` for runtime
hashes, verification and limits. Static chart/tools only.

2026-09-20: frozen technical authoring delivery complete. Ready for root publication;
no runtime integration is claimed by this package. See `HANDOFF.md` for exact hashes.
Key `frontier_field_command_table`; asset `prop.frontier.field_command_table`.

Design: universal 1.80 m × 1.00 m oak trestle table with mortised bearers,
through-stretcher, knee braces, thick planks and breadboard ends. An original
Aegis/Riftbound campaign sketch occupies fitted parchment; weights, dividers,
ink pot, writing tool and closed dispatch folio have physical supports.
The front is negative Blender Y / positive runtime Z. Standing point Z 1.08 m
must admit a 0.5 m actor. Static scenery only.

Required before freeze: three reduced literal GLBs, six packed editable masters,
closed structural surfaces, meaningful joinery/support contacts, UV/tangent/PBR
validation, exact-import neutral/detail/rear/joinery/gameplay and LOD views,
measured collision masses and ground feet, Khronos zero errors/warnings.

Root owns publication, manifests, placement and GM/gameplay verification. Existing
published rack, farmer and herbalist packages remain immutable. Geometry and map
are original to this package; copied explicit polygon helpers are documented in
`provenance.json`.

Checkpoint: all three literal exports and six packed masters are saved. Current
counts are 27,534 / 13,366 / 5,274 triangles and 8,276,640 / 3,095,432 / 962,160
bytes, six material batches each. Source/finished master closure and packed-image
checks pass. The 300 per-LOD contacts pass after proving retained named final
surface witnesses match the actual GLB imports within 2 micrometres. This fixed
an initial 2.1–2.5 mm gap between the finished knee braces and tabletop by extending
the brace tenons into real receiving mortises. Timber UVs now follow grain after
Boolean cutting, preventing cutter charts from making rectangular top-board
patches. The original map now uses the parchment's actual aspect and includes
both factions' side routes. Final view set and complete publication gate remain
in progress; package is not frozen or ready yet.

Final checkpoint supersedes the in-progress note above: all mandatory gates and
all nine actual-import views pass and have been inspected. The texture seam was
removed using genuinely periodic material fields. Final GLBs are 27,534 / 13,366 /
5,274 triangles and 8,343,904 / 3,117,428 / 968,028 bytes. All 300 actual-import
fitting contacts pass with zero vertex deviation between the retained named
finished surfaces and literal GLBs; worst measured support gap is 0.452 mm.
Each of six source/final masters has all 18 PBR images packed. Closed-surface,
UV/tangent, material batching, genuine LOD reduction, source-byte and image-byte
gates pass. Khronos has zero errors/warnings at all LODs (one informational note
for each intentionally non-power-of-two map image).

Six collision masses cover all 94 LOD0 visible components exactly once. Two skids
have eight vertices at Z0 each; standing point +Z1.08 clears the half-metre actor.
The source and finished JSON audit evidence is minified. No more geometry changes
are planned. Root may publish immediately, then perform live GM/world placement
and collision acceptance. Tabletop walking, tool pickup and map interaction are
not implemented or claimed.
